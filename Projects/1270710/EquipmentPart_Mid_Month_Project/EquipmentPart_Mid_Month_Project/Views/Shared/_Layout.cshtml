<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - My ASP.NET Application</title>

    <link href="~/Content/bootstrap.min.css" rel="stylesheet" type="text/css" />
    <script src="~/Scripts/bootstrap.bundle.js"></script>

    <style>
        body {
            padding-top: 0px;
        }
    </style>
    @RenderSection("styles", false)
</head>
<body>
    <div class="navbar bg-info text-white navbar-expand-sm navbar-fixed-top shadow-sm border-bottom">
        <div class="container-fluid">
            <span class="navbar-brand">EquipmentPart</span>
            <div class="navbar-collapse collapse">
                <ul class="nav navbar-nav">
                    <li class="nav-item">
                        @Html.ActionLink("Home", "Index", "Home", new { }, new { @class = "nav-link" })
                    </li>
                    <li class="nav-item">
                        @Html.ActionLink("MasterDetail", "Index", "Equipment", new { }, new { @class = "nav-link" })
                    </li>
                    <li class="nav-item">
                        @Html.ActionLink("PartDetail", "Index", "PartDetail", new { }, new { @class = "nav-link" })
                    </li>
                    <li class="nav-item">
                        @Html.ActionLink("Customer", "Index", "Customer", new { }, new { @class = "nav-link" })
                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            Reports
                        </a>
                        <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <li>@Html.ActionLink("Simple Report", "PartReport", "Crystal", new { }, new { @class = "dropdown-item" })</li>
                            <li>@Html.ActionLink("Report With Image", "Index", "Crystal", new { }, new { @class = "dropdown-item" })</li>
                        </ul>
                    </li>
                </ul>
                <ul class="nav navbar-nav ms-auto ">
                    @if (Request.IsAuthenticated)
                    {
                        <li class="nav-item">
                            @Html.ActionLink("Logout", "Logout", "Account", new { }, new { @class = "nav-link" })
                        </li>
                    }
                    else
                    {
                        <li class="nav-item">
                            @Html.ActionLink("Register", "Register", "Account", new { }, new { @class = "nav-link" })
                        </li>
                        <li class="nav-item">
                            @Html.ActionLink("Login", "Login", "Account", new { }, new { @class = "nav-link" })
                        </li>
                    }
                </ul>
            </div>
        </div>
    </div>
    <main class="container-fluid" style="min-height:calc(100vh - 95px)">
        @RenderBody()
    </main>
    <footer class="container-fluid bg-warning">
        <p>&copy; @DateTime.Today.ToString("MMM dd, yyyy") - My ASP.NET Application</p>
    </footer>

    @RenderSection("scripts", false)
</body>
</html>