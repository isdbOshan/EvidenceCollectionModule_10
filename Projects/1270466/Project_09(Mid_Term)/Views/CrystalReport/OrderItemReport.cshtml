@model IEnumerable<Project_09_Mid_Term_.ViewModels.OrderItemReport>
@{
    ViewBag.Title = "OrderItemReport";
}

<div class="row">
    <div class="col-12">
        <h3>Customers</h3>

        <table class="table table-bordered">
            <thead class="table-light">
                <tr>

                    <th>Computer Name</th>

                </tr>
            </thead>
            <tbody>
                @foreach (var g in Model.GroupBy(x => x.ComputerName).ToList())
                {
                    <tr>
                        <td>
                            @g.Key
                        </td>

                    </tr>
                    <tr>
                        <td>&nbsp;</td>
                        <td colspan="6">
                            <h4>Orders</h4>
                            <table class="table table-bordered">
                                <thead>
                                    <tr>
                                        <th>Order ItemId</th>
                                        <th>Quantity Date</th>
                                        <th>Order Date</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var q in g)
                                    {
                                        <tr>
                                            <td>@q.OrderItemId</td>
                                            <td>@q.Quantity</td>
                                            <td>@q.OrderDate</td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
    <div class="col-12 text-center">
        @Html.ActionLink("PDF", "OrderItemPDF", "CrystalReport", new { }, new { @class = "btn btn-primary btn-sm" })
        @Html.ActionLink("Excel", "OrderItemExcel", "CrystalReport", new { }, new { @class = "btn btn-secondary btn-sm" })
    </div>
</div>


